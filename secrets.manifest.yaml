secrets:
  - name: HA_LONG_LIVED_TOKEN
    description: "Long-lived access token for Home Assistant REST API"
    required: true
  - name: ROSIE_DISCORD_BOT_TOKEN
    description: "Discord Bot Token for Doorman presence and handoff"
    required: true
